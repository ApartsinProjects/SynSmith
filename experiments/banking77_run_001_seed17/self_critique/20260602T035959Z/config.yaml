schema_path: examples/banking77/schema.yaml
real_examples_path: E:\Projects\PromptForge\experiments\_splits\real_train.jsonl
domain: banking77_card_payment
task_description: 'Classify a banking customer support message into one of ten card/payment
  intents.

  The classes share heavy vocabulary overlap (card, payment, transaction, declined,

  blocked); requires fine semantic distinctions rather than keyword matching.

  '
iterations: 3
samples_per_iteration: 16
initial_prompt: 'Generate realistic banking customer-support messages that match the
  requested intent

  and style. The example should be plausible for a customer support channel

  (email, chat, in-app form). Keep it short to medium length. Output a JSON

  object with the sample id, the text, and the attribute values.

  '
seed: 17
